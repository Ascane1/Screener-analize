# Trading Bot для Bybit

Автоматический торговый бот для биржи Bybit, использующий **Kernel Regression** и **Relative Performance Analysis** для генерации торговых сигналов.

---

## 📊 Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                        Trading Bot                          │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐   ┌─────────────┐   ┌─────────────────┐   │
│  │  Screener   │──▶│  Analyzer   │──▶│    Executor     │   │
│  │  (скрининг) │   │  (сигналы)  │   │  (исполнение)   │   │
│  └─────────────┘   └─────────────┘   └─────────────────┘   │
│         │                 │                   │             │
│         ▼                 ▼                   ▼             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              DataManager (API Bybit)                │   │
│  ├─────────────────────────────────────────────────────┤   │
│  │           External Data (CoinGecko API)             │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 Структура проекта

| Файл | Описание |
|------|----------|
| [`main.py`](trading_bot/main.py) | Главный модуль, цикл сканирования (реальная торговля) |
| [`main_dry_run.py`](main_dry_run.py) | DRY RUN версия бота (без реальных сделок) |
| [`config.py`](trading_bot/config.py) | Конфигурация (загружается из .env файлов) |
| [`data_manager.py`](trading_bot/data_manager.py) | Работа с API Bybit и CoinGecko |
| [`screener.py`](trading_bot/screener.py) | Отбор топ монет по волатильности |
| [`analyzer.py`](trading_bot/analyzer.py) | Анализ (Kernel + Relative Performance) |
| [`executor.py`](trading_bot/executor.py) | Исполнение сделок и управление рисками |
| [`kernels.py`](trading_bot/kernels.py) | Функции ядер регрессии (16 типов) |
| [`utils.py`](trading_bot/utils.py) | Утилиты, уведомления и логирование |
| [`test_connection.py`](test_connection.py) | Тест подключения к API |
| [`test_screener.py`](test_screener.py) | Тест модуля скринера |
| [`test_analyzer.py`](test_analyzer.py) | Тест модуля анализатора |
| [`test_executor.py`](test_executor.py) | Тест модуля исполнителя |

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

Зависимости:
- `pybit` - клиент API Bybit
- `python-dotenv` - загрузка переменных окружения
- `pandas` - обработка данных
- `numpy` - вычисления
- `requests` - HTTP запросы (уведомления + CoinGecko)

### 2. Настройка конфигурации

Конфигурация загружается из `.env` файлов в корне проекта:

#### API настройки ([`.env.api`](.env.api))
```bash
BYBIT_API_KEY=ваш_api_key
BYBIT_API_SECRET=ваш_api_secret
BYBIT_ENDPOINT=https://api-demo.bybit.com  # или https://api.bybit.com
TESTNET=False  # True для тестовой сети
```

#### Параметры ядра ([`.env.kernel`](.env.kernel))
```bash
KERNEL_TYPE=Epanechnikov  # Тип ядра (см. список ниже)
BANDWIDTH=20              # Период сглаживания
SOURCE=close              # Источник данных
```

#### Параметры относительной производительности ([`.env.performance`](.env.performance))
```bash
BENCHMARK_SYMBOL=BTCUSDT           # Бенчмарк (BTCUSDT или TOTAL для всего рынка)
DISPLAY_MODE=Standardized           # Режим: Net Returns, Rescaled, Standardized
SESSION_LENGTH=48                    # Длина сессии в барах
```

#### Торговые параметры ([`.env.trading`](.env.trading))
```bash
TIMEFRAME=15              # Таймфрейм (минуты: 1, 5, 15, 60)
SL_PERCENT=5.0            # Stop Loss %
TP_PERCENT=5.0            # Take Profit %
LEVERAGE=15               # Плечо
RISK_PER_TRADE=0.01       # 1% от депозита
MAX_POSITIONS=5           # Максимум позиций
```

#### Параметры скринера ([`.env.screener`](.env.screener))
```bash
TOP_GAINERS_COUNT=5       # Топ растущих
TOP_LOSERS_COUNT=5        # Топ падающих
MIN_VOLUME_24H=1000000    # Минимальный объём ($)
MIN_PRICE_CHANGE=2.0      # Минимальное изменение %
```

#### Системные параметры ([`.env.system`](.env.system))
```bash
SCAN_INTERVAL_SECONDS=60  # Интервал сканирования
KLINES_LIMIT=200          # Количество свечей для анализа
```

#### Исключения ([`.env.exclusions`](.env.exclusions))
```bash
EXCLUDED_SYMBOLS=USDCUSDT,BTCUSDT  # Исключённые символы
```

### 3. Тестирование подключения

```bash
python test_connection.py
```

### 4. Запуск в DRY RUN режиме (без реальных сделок)

```bash
python main_dry_run.py
```

### 5. Запуск бота (реальная торговля)

```bash
python -m trading_bot.main
```

---

## 📈 Алгоритм работы

### Цикл сканирования (каждые 60 секунд)

1. **Скрининг** ([`Screener.get_top_movers()`](trading_bot/screener.py:15))
   - Получает тикеры USDT Perpetual.
   - Фильтрует по объёму (>1M), изменению (>2%) и исключениям.
   - Исключает монеты, по которым уже открыты позиции.

2. **Анализ** ([`MultiKernelAnalyzer.analyze()`](trading_bot/analyzer.py:243))
   - Использует **hlc3** (High+Low+Close)/3 для всех расчетов.
   - Рассчитывает **Kernel Regression** для сглаживания цены.
   - Сравнивает актив с бенчмарком (BTC или TOTAL через CoinGecko).
   - Определяет зону: **Green Zone** (лучше рынка) или **Red Zone** (хуже рынка).
   - Генерирует сигналы при пересечении (crossover) Kernel MA:
     - **LONG**: Kernel пересекает цену снизу вверх + Green Zone.
     - **SHORT**: Kernel пересекает цену сверху вниз + Red Zone.

3. **Исполнение** ([`Executor.execute()`](trading_bot/executor.py:116))
   - Проверяет лимит позиций (`MAX_POSITIONS`).
   - Рассчитывает размер позиции на основе риска (`RISK_PER_TRADE`) и расстояния до SL.
   - Округляет цену и количество согласно спецификациям биржи (`tickSize`, `qtyStep`).
   - Устанавливает плечо и открывает Market-ордер с автоматическим SL/TP.

4. **Мониторинг**
   - Логирует состояние позиций, PnL и текущую зону (🟢/🔴).

---

## 🎯 Параметры ядер

В [`kernels.py`](trading_bot/kernels.py) доступны 16 типов ядер:

| Тип | Характеристика |
|-----|----------------|
| `Epanechnikov` | Оптимальное MSE, рекомендуется |
| `Gaussian` | Гладкое, популярное |
| `Laplace` | Быстрое, острый пик |
| `Triangular` | Простое, линейное |
| `Quartic` | Сглаженное |
| `Cosine` | Для периодических данных |
| `Exponential` | Экспоненциальное затухание |
| `Cauchy` | Тяжёлые хвосты |
| `Logistic` | Логистическая функция |
| `Log Logistic` | Лог-логистическая |
| `Sinc` | Синус-кардиоид |
| `Silverman` | Серебряное правило |
| `Tent` | Треугольная палатка |
| `Wave` | Волновая |
| `Power` | Степенная |
| `Morters` | Мортенс |

---

## ⚠️ Управление рисками

| Параметр | Значение по умолчанию | Описание |
|----------|----------|----------|
| `RISK_PER_TRADE` | 1% | Риск от баланса на одну сделку |
| `MAX_POSITIONS` | 5 | Максимальное кол-во активных сделок |
| `SL_PERCENT` | 5.0% | Фиксированный Stop Loss |
| `TP_PERCENT` | 5.0% | Фиксированный Take Profit |
| `LEVERAGE` | 15x | Используемое плечо |

---

## 🔔 Уведомления

### Telegram
```python
from trading_bot.utils import TelegramNotifier

telegram = TelegramNotifier(bot_token="token", chat_id="id")
telegram.send_signal("BUY", "ETHUSDT", 2000, 1900, 2100, reason="Kernel Cross Up")
```

### Discord
```python
from trading_bot.utils import DiscordNotifier

discord = DiscordNotifier(webhook_url="url")
discord.send_signal("SELL", "BTCUSDT", 50000, 52500, 47500, reason="Kernel Cross Down")
```

---

## 📝 Логирование сделок

Бот автоматически сохраняет историю сделок в `trades.json` через [`TradeLogger`](trading_bot/utils.py:451).
Вы можете генерировать отчеты:
```python
from trading_bot.utils import TradeLogger
logger = TradeLogger("trades.json")
print(logger.generate_report())
```

---

## 🧪 Тестирование

Подробная инструкция в [`test.md`](trading_bot/test.md).

### Тесты модулей:
```bash
python test_connection.py  # Проверка API
python test_screener.py    # Проверка выбора монет
python test_analyzer.py    # Проверка логики сигналов
python test_executor.py    # Проверка выставления ордеров
```

### DRY RUN режим:
```bash
python main_dry_run.py
```
Позволяет увидеть, какие сделки открыл бы бот, без риска для баланса.

---

## 🔒 Безопасность

- **API ключи**: Никогда не коммитьте `.env` файлы.
- **Testnet**: Всегда начинайте с `TESTNET=True`.
- **Риски**: Начинайте с минимального `RISK_PER_TRADE`.

---

## 📄 Лицензия

MIT License
