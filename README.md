# Trading Bot для Bybit

Автоматический торговый бот для биржи Bybit, использующий **Kernel Regression** и **Relative Performance Analysis** для генерации торговых сигналов.

---

## 📊 Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                        Trading Bot                          │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐   ┌─────────────┐   ┌─────────────────┐   │
│  │  Screener   │──▶│  Analyzer   │──▶│    Executor     │   │
│  │  (скрининг) │   │  (сигналы)  │   │  (исполнение)   │   │
│  └─────────────┘   └─────────────┘   └─────────────────┘   │
│         │                 │                   │             │
│         ▼                 ▼                   ▼             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              DataManager (API Bybit)                │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 Структура проекта

| Файл | Описание |
|------|----------|
| [`main.py`](trading_bot/main.py) | Главный модуль, цикл сканирования (реальная торговля) |
| [`main_dry_run.py`](main_dry_run.py) | DRY RUN версия бота (без реальных сделок) |
| [`config.py`](trading_bot/config.py) | Конфигурация (загружается из .env файлов) |
| [`data_manager.py`](trading_bot/data_manager.py) | Работа с API Bybit |
| [`screener.py`](trading_bot/screener.py) | Отбор топ монет |
| [`analyzer.py`](trading_bot/analyzer.py) | Анализ и генерация сигналов |
| [`executor.py`](trading_bot/executor.py) | Исполнение сделок |
| [`kernels.py`](trading_bot/kernels.py) | Функции ядер регрессии |
| [`utils.py`](trading_bot/utils.py) | Утилиты и уведомления |
| [`test_connection.py`](test_connection.py) | Тест подключения |
| [`test_screener.py`](test_screener.py) | Тест скринера |
| [`test_analyzer.py`](test_analyzer.py) | Тест анализатора |
| [`test_executor.py`](test_executor.py) | Тест исполнителя |

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

Зависимости:
- `pybit` - клиент API Bybit
- `python-dotenv` - загрузка переменных окружения
- `pandas` - обработка данных
- `numpy` - вычисления
- `requests` - HTTP запросы (для уведомлений)

### 2. Настройка конфигурации

Конфигурация загружается из `.env` файлов в корне проекта:

#### API настройки ([`.env.api`](.env.api))
```bash
BYBIT_API_KEY=ваш_api_key
BYBIT_API_SECRET=ваш_api_secret
BYBIT_ENDPOINT=https://api-demo.bybit.com  # или https://api.bybit.com
TESTNET=False  # True для тестовой сети
```

#### Параметры ядра ([`.env.kernel`](.env.kernel))
```bash
KERNEL_TYPE=Epanechnikov  # Тип ядра
BANDWIDTH=20              # Период сглаживания
SOURCE=close              # Источник данных
```

#### Параметры относительной производительности ([`.env.performance`](.env.performance))
```bash
BENCHMARK_SYMBOL=BTCUSDT           # Бенчмарк
DISPLAY_MODE=Standardized           # Режим отображения
SESSION_LENGTH=48                    # Длина сессии в барах
```

#### Торговые параметры ([`.env.trading`](.env.trading))
```bash
TIMEFRAME=15              # Таймфрейм (минуты)
SL_PERCENT=1.0            # Stop Loss %
TP_PERCENT=2.0            # Take Profit %
LEVERAGE=15               # Плечо
RISK_PER_TRADE=0.01       # 1% от депозита
MAX_POSITIONS=5           # Максимум позиций
```

#### Параметры скринера ([`.env.screener`](.env.screener))
```bash
TOP_GAINERS_COUNT=5       # Топ растущих
TOP_LOSERS_COUNT=5        # Топ падающих
MIN_VOLUME_24H=1000000    # Минимальный объём
MIN_PRICE_CHANGE=2.0      # Минимальное изменение %
```

#### Системные параметры ([`.env.system`](.env.system))
```bash
SCAN_INTERVAL_SECONDS=60  # Интервал сканирования
KLINES_LIMIT=200          # Количество свечей
```

#### Исключения ([`.env.exclusions`](.env.exclusions))
```bash
EXCLUDED_SYMBOLS=USDCUSDT,BTCUSDT  # Исключённые символы
```

### 3. Тестирование подключения

```bash
python test_connection.py
```

### 4. Запуск в DRY RUN режиме (без реальных сделок)

```bash
python main_dry_run.py
```

### 5. Запуск бота (реальная торговля)

Для корректного импорта модулей пакет `trading_bot` должен запускаться как модуль из корня проекта:

```bash
python -m trading_bot.main
```

---

## 📈 Алгоритм работы

### Цикл сканирования (каждые 60 секунд)

1. **Скрининг** ([`Screener.get_top_movers()`](trading_bot/screener.py:15))
   - Получает все тикеры USDT Perpetual
   - Отбирает топ-5 растущих и топ-5 падающих
   - Фильтрует по объёму (>1M) и изменению (>2%)
   - Исключает символы с открытыми позициями

2. **Анализ** ([`MultiKernelAnalyzer.analyze()`](trading_bot/analyzer.py:184))
   - Рассчитывает Kernel Regression для каждой монеты
   - Сравнивает с бенчмарком (BTCUSDT)
   - Определяет **Green Zone** (опережает бенчмарк) или **Red Zone**
   - Генерирует сигналы при crossover:
     - **LONG**: Kernel пересекает цену снизу вверх + Green Zone
     - **SHORT**: Kernel пересекает цену сверху вниз + Red Zone

3. **Исполнение** ([`Executor.execute()`](trading_bot/executor.py:84))
   - Сортирует сигналы по силе
   - Исполняет максимум 2 сделки за цикл
   - Расчёт размера позиции: 1% от депозита / расстояние до SL
   - Устанавливает плечо, открывает Market-ордер с SL/TP

4. **Мониторинг**
   - Логирует открытые позиции
   - Показывает PnL и статус зоны (🟢/🔴)

---

## 🎯 Параметры ядер

В [`kernels.py`](trading_bot/kernels.py) доступны 16 типов ядер:

| Тип | Характеристика |
|-----|----------------|
| `Epanechnikov` | Оптимальное MSE, рекомендуется |
| `Gaussian` | Гладкое, популярное |
| `Laplace` | Быстрое, острый пик |
| `Triangular` | Простое, линейное |
| `Quartic` | Сглаженное |
| `Cosine` | Для периодических данных |
| `Exponential` | Экспоненциальное затухание |
| `Cauchy` | Тяжёлые хвосты |
| `Logistic` | Логистическая функция |
| `Log Logistic` | Лог-логистическая |
| `Sinc` | Синус-кардиоид |
| `Silverman` | Серебряное правило |
| `Tent` | Треугольная палатка |
| `Wave` | Волновая |
| `Power` | Степенная |
| `Morters` | Мортенс |

---

## ⚠️ Управление рисками

| Параметр | Значение | Описание |
|----------|----------|----------|
| `RISK_PER_TRADE` | 1% | Максимальный риск на сделку |
| `MAX_POSITIONS` | 5 | Максимум открытых позиций |
| `SL_PERCENT` | 1.0% | Stop Loss |
| `TP_PERCENT` | 2.0% | Take Profit (Risk/Reward 1:2) |
| `LEVERAGE` | 15x | Плечо |

---

## 🔔 Уведомления

### Telegram

```python
from trading_bot.utils import TelegramNotifier

telegram = TelegramNotifier(
    bot_token="your_bot_token",
    chat_id="your_chat_id"
)

telegram.send_signal("BUY", "ETHUSDT", 2000, 1980, 2040)
```

### Discord

```python
from trading_bot.utils import DiscordNotifier

discord = DiscordNotifier(
    webhook_url="your_webhook_url"
)

discord.send_signal("SELL", "BTCUSDT", 50000, 50500, 49000)
```

---

## 📝 Логирование сделок

```python
from trading_bot.utils import TradeLogger, Trade, generate_trade_id

logger = TradeLogger("trades.json")

trade = Trade(
    id=generate_trade_id(),
    symbol="BTCUSDT",
    side="BUY",
    entry_price=50000,
    exit_price=None,
    size=0.1,
    pnl=None,
    pnl_percent=None,
    entry_time="2024-01-01 12:00:00",
    exit_time=None,
    stop_loss=49500,
    take_profit=51000,
    reason="Kernel crossover UP + Green Zone",
    status="open"
)

logger.add_trade(trade)
print(logger.generate_report())
```

---

## 🧪 Тестирование

Следуйте инструкции в [`test.md`](trading_bot/test.md):

1. Проверка окружения (Python 3.9+, зависимости)
2. Тестирование модулей по отдельности
3. Интеграционное тестирование
4. Запуск на testnet
5. Переход на production

### Тестирование отдельных модулей

```bash
# Тест подключения
python test_connection.py

# Тест скринера
python test_screener.py

# Тест анализатора
python test_analyzer.py

# Тест исполнителя
python test_executor.py
```

### DRY RUN режим

Для тестирования без реальных сделок используйте [`main_dry_run.py`](main_dry_run.py):

```bash
python main_dry_run.py
```

Этот режим:
- Выполняет полный цикл анализа
- Показывает сигналы
- Рассчитывает размер позиции
- Но НЕ исполняет реальные ордера

---

## 🔒 Безопасность

- **API ключи**: Храните в `.env` файлах (не коммитить в git)
- **Testnet**: Всегда тестируйте на тестовой сети сначала
- **Риск**: Не превышайте 1-2% на сделку
- **Мониторинг**: Следите за логами в реальном времени
- **Исключения**: Добавьте `.env*` в `.gitignore`

---

## 📄 Лицензия

MIT License
